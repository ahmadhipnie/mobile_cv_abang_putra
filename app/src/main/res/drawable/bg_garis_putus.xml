<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <size android:width="1dp" android:height="1dp" />
    <stroke
        android:width="4dp"
        android:color="@color/colorPrimary" /> <!-- Ganti "your_color" dengan warna yang diinginkan -->
    <stroke
        android:width="2dp"
        android:color="@color/colorPrimary"
        android:dashWidth="4dp"
        android:dashGap="4dp" /> <!-- Ganti "your_background_color" dengan warna latar belakang yang diinginkan -->
</shape>
